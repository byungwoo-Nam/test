name: work

on:
  push:
    branches:
      - main

jobs:
  Slack Send:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - name: work step
        uses: ./github/actions/slackSend
        with:
          status: true
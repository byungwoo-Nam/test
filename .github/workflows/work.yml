name: work

on:
  push:
    branches:
      - main

jobs:
  Work:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: work step
        uses: ./.github/actions/slackSend
        with:
          status: ${{job.status}}